#include<stdio.h>
#include<math.h>
int main()
{
        float n,m;
        printf("Enter no for finding root:");
        scanf("%f",&n);
        m=sqrt(n);
        printf("Square root of %f is = %3f\n",n,m);
        return 0;
}
